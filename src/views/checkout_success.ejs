<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Pago Completado - GedotsHouse</title>
    <link rel="stylesheet" href="/css/store.css">

    <style>
        .success-container {
            max-width: 900px;
            margin: 4rem auto;
            background: white;
            padding: 3rem;
            border-radius: var(--radius-2xl);
            box-shadow: var(--shadow-2xl);
            text-align: center;
        }

        .order-box {
            margin-top: 2rem;
            text-align: left;
            background: var(--bg-gray-50);
            padding: 2rem;
            border-radius: var(--radius-xl);
        }

        .order-box h3 {
            margin-bottom: 1rem;
            font-weight: 900;
        }

        .item-row {
            padding: 10px 0;
            border-bottom: 1px solid var(--bg-gray-100);
        }
        .item-row:last-child {
            border-bottom: none;
        }

        .btn-primary {
            margin-top: 2rem;
        }
    </style>
</head>
<body>

<div class="success-container">

    <h1 class="checkout-title">🎉 ¡Pago Completado!</h1>

    <p style="font-size:1.2rem; margin-top:1rem;">
        Tu orden <strong>#<%= order.order_number %></strong> ha sido generada correctamente.
    </p>

    <div class="order-box">
        <h3>📦 Detalles del Pedido</h3>

        <p><strong>Fecha:</strong> <%= order.created_at.toLocaleString() %></p>
        <p><strong>Estado:</strong> <%= order.status %></p>
        <p><strong>Método de pago:</strong> <%= order.payment_method %></p>

        <hr style="margin: 1.5rem 0;">

        <h3>🛒 Productos Comprados</h3>

        <% items.forEach(item => { %>
            <div class="item-row">
                <strong><%= item.product_name %></strong>  
                <br>
                SKU: <%= item.product_sku %>  
                <br>
                Cantidad: <%= item.quantity %>  
                <br>
                Precio: $<%= item.unit_price %>  
                <br>
                Subtotal: <strong>$<%= item.subtotal %></strong>
            </div>
        <% }) %>

        <hr style="margin: 1.5rem 0;">

        <h3>Total Pagado:</h3>
        <p style="font-size:1.8rem; font-weight:900;">
            $<%= order.total_amount %>
        </p>
    </div>

    <a href="/account" class="btn-epic-primary btn-primary">
        Ver mis órdenes
    </a>

    <a href="/" class="btn-hero-secondary" style="display:block; margin-top:1.5rem;">
        Seguir comprando
    </a>

</div>

</body>
</html>